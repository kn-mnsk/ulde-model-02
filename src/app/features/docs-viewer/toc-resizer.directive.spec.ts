import { TocResizerDirective } from './toc-resizer.directive';

describe('TocResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new TocResizerDirective();
    expect(directive).toBeTruthy();
  });
});
